package dev.teogor.verbatim.compiler

import org.gradle.api.Plugin
import org.gradle.api.Project

class VerbatimCompilerPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        with(target) {
            // TODO: Implement compiler plugin registration logic
        }
    }
}
